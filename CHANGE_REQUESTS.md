# CHANGE_REQUESTS

## 제품 방향과 다를 가능성이 있는 부분

현재 구현은 요청한 MVP 조건을 만족하는 방향으로 빠르게 세운 앱입니다. 하지만 "BeingFlow"가 지향할 수 있는 차분한 루틴 경험과 비교하면 아래 부분은 방향 확인이 필요합니다.

## 1. 루틴 화면이 아직 기록 도구보다 타이머 앱처럼 보일 수 있음

원하는 방향이 "포모도로보다 관찰-기록 루프"라면, 오늘 화면의 중심은 단순 시작 버튼보다 오늘의 흐름이어야 합니다.

현재는:

- 명상 5분 -> 작업 40분 -> 명상 5분 텍스트
- 새 루틴 시작 버튼
- 최근 기록 일부
- 일기 생성 버튼

원하는 방향일 가능성이 높은 형태:

- 오늘의 루프 진행 상태가 먼저 보임
- 각 단계 아래에 기록 상태가 붙음
- "다음 행동"이 더 명확함
- 완료된 루프가 하루 기록의 기본 단위가 됨

## 2. 타이머 종료 후 전환 경험이 너무 기능적임

현재 타이머 종료 후 음성 기록 화면으로 상태가 바뀌지만, 제품적으로는 이 순간이 BeingFlow의 핵심입니다.

바꿀 가능성이 있는 방향:

- 종료 직후 "멈춤" 느낌을 주는 전용 전환 화면
- 명상 후와 작업 후의 질문 톤 차별화
- 바로 녹음 버튼을 누르게 만드는 큰 CTA
- "다음 세그먼트는 기록 후 시작됩니다"를 분명히 표시

## 3. 음성 메모가 아직 신뢰감을 충분히 주지 못함

현재 녹음/정지/저장 기능은 있지만, 사용자는 다음을 확신하기 어렵습니다.

- 지금 정말 녹음 중인지
- 몇 초 녹음했는지
- 오디오 파일이 저장됐는지
- 전사가 실패했는지, 아직 기다리는 중인지
- 나중에 전사가 다시 시도되는지

BeingFlow가 매일 쓰는 기록 앱이라면 음성 메모는 가장 신뢰감이 높아야 하는 영역입니다.

## 4. "건너뛰기"가 너무 쉬울 수 있음

기록을 남기는 것이 제품의 핵심 가치라면, 건너뛰기는 가능하되 약간 의식적인 행동이어야 합니다.

현재는:

- 건너뛰기 버튼을 누르면 바로 다음 단계로 진행
- skip reason 없음
- 나중에 빈 기록으로 남지 않음

방향 확인:

- 건너뛴 사실도 세그먼트 기록으로 남길지
- "정말 건너뛸까요?"가 필요한지
- 텍스트 한 줄만 남기고 넘어가는 대안이 필요한지

## 5. 오늘 기록 화면이 루틴 맥락을 잃음

현재 오늘 기록은 노트 리스트입니다. 하지만 BeingFlow의 핵심은 개별 노트보다 "루프"입니다.

원하는 방향일 가능성이 높은 형태:

- 세션별 그룹
- 명상 1, 작업, 명상 2 순서 고정 표시
- 각 단계의 오디오/전사/스킵 상태 표시
- 일기에 포함된 기록 표시

## 6. 일기 생성이 OpenAI 설정에 너무 강하게 묶임

현재 일기 생성은 OpenAI 일기 생성이 켜져 있고 API 키가 있어야 의미 있게 동작합니다. 제품 관점에서는 API 키가 없어도 "오늘 기록 모음" 또는 "초안" 정도는 보여줄 수 있습니다.

방향 확인:

- OpenAI가 없을 때도 로컬 요약 형태를 만들지
- 일기 화면에서 "API 키 없음"보다 "오늘의 원자료"를 먼저 보여줄지
- 사용자가 매일 기록을 쌓는 감각을 OpenAI 없이도 제공할지

## 7. OpenAI 전사 재시도 위치가 숨겨져 있음

OpenAI 전사는 수동 일기 생성 또는 daily worker 안에서 pending 오디오를 처리합니다. 사용자는 이 사실을 거의 알 수 없습니다.

바꿀 가능성이 있는 방향:

- 오늘 기록 화면에 "전사 재시도" 버튼
- pending 기록 수 표시
- 전사 실패 사유 표시
- OpenAI 전송 전 확인 UX

## 8. 설정 화면이 MVP 기능 나열에 가까움

현재 설정은 필요한 필드가 모두 있지만, 제품적으로는 민감한 설정과 루틴 설정이 한 화면에 섞여 있습니다.

나눌 가능성이 있는 구조:

- 루틴 시간
- 일기 생성
- OpenAI 연결
- 개인정보/저장소
- 데이터 관리

## 9. 로컬 저장 정책이 사용자 언어로 충분히 드러나지 않음

README에는 저장 정책이 있지만 앱 안에서는 요약 텍스트 한 문단뿐입니다.

제품 방향상 중요한 질문:

- 오디오를 계속 보관할 것인가
- 전사 후 자동 삭제 옵션이 필요한가
- 전체 데이터 삭제가 필요한가
- 특정 날짜만 삭제할 수 있어야 하는가

## 10. 앱의 감정 톤이 아직 덜 정돈됨

Korean UI labels are concise, but the experience still feels like a functional prototype. BeingFlow가 명상과 작업 사이를 다루는 앱이라면 다음 톤이 더 맞을 수 있습니다.

- 압박보다 관찰
- 성과보다 기록
- 완료보다 흐름
- "잘했어요"보다 "기록했습니다"

