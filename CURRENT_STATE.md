# CURRENT_STATE

## 한 줄 상태

현재 앱은 "명상 -> 음성 기록 -> 작업 -> 음성 기록 -> 명상 -> 음성 기록 -> 오늘 일기 생성"이라는 BeingFlow의 기본 루프를 실행할 수 있는 Android MVP입니다. 다만 제품 경험은 아직 "작동하는 기술 시연"에 가깝고, 사용자가 매일 자연스럽게 쓰는 루틴 도구로 느껴지기에는 흐름 안내와 기록 관리가 단순합니다.

## 실제로 가능한 일

- 사용자는 오늘 화면에서 새 루틴을 시작할 수 있습니다.
- 루틴은 고정 순서입니다: 명상, 작업, 명상.
- 기본 시간은 명상 5분, 작업 40분입니다.
- 설정에서 각 시간을 분 단위로 바꿀 수 있습니다.
- 명상 단계에서는 좌선 또는 걷기 명상을 선택할 수 있습니다.
- 작업 단계에서는 작업 이름을 입력할 수 있습니다.
- 타이머가 끝나면 해당 세그먼트 상태가 음성 기록 필요 상태로 바뀝니다.
- 음성 기록을 저장하거나 건너뛰어야 다음 세그먼트가 시작됩니다.
- 음성 기록 화면은 로컬 오디오 파일 저장을 시도하고, 동시에 Android SpeechRecognizer로 실시간 전사를 시도합니다.
- 전사가 비어 있으면 전사 대기 중 상태로 저장할 수 있습니다.
- 오늘 기록 화면에서 오늘 생성된 음성 기록 목록을 볼 수 있습니다.
- OpenAI API 키와 일기 생성 옵션을 켜면 오늘 기록 기반 일기를 생성할 수 있습니다.
- OpenAI 음성 전사 옵션을 켜면 일기 생성 전 pending 오디오 기록을 전사하려고 시도합니다.
- WorkManager가 설정 시간에 일일 일기 생성을 예약합니다.

## UX 흐름

현재 화면 구조는 하단 탭 5개입니다.

- 오늘
- 타이머
- 기록
- 일기
- 설정

오늘 화면은 루틴 시작점 역할을 합니다. 진행 중인 루틴이 있으면 현재 단계로 이동하는 버튼을 보여줍니다. 타이머가 끝나면 앱은 자동으로 타이머 탭에서 음성 기록 화면을 보여주도록 상태를 전환합니다.

흐름 자체는 맞지만, 현재 UX는 루틴의 현재 위치를 충분히 시각화하지 않습니다. 사용자는 "지금 3단계 중 몇 번째인지", "앞으로 무엇이 남았는지", "이 기록을 남기면 다음에 무엇이 시작되는지"를 텍스트 일부로만 추론해야 합니다.

## 타이머 경험

타이머는 세그먼트의 `expectedEndAt`을 저장하고 남은 시간을 현재 시각 기준으로 계산합니다. 따라서 앱이 pause/resume되어도 단순 in-memory countdown보다 안전합니다.

타이머 종료 시:

- 세그먼트 상태가 `NOTE_REQUIRED`가 됩니다.
- 알림, 소리, 진동을 시도합니다.
- 다음 세그먼트로 자동 진행하지 않습니다.
- 음성 기록 저장 또는 건너뛰기 후 다음 세그먼트가 시작됩니다.

현재 타이머 경험의 성격은 "정직한 MVP"입니다. foreground service 기반의 장시간 타이머 UX는 아니고, 잠금화면/백그라운드에서 사용자가 얼마나 명확하게 복귀할 수 있는지는 아직 약합니다.

## 음성 메모 경험

음성 기록 화면은 다음을 제공합니다.

- 녹음
- 정지
- 다시
- 저장하고 계속
- 건너뛰기
- 전사 텍스트 편집

오디오는 앱 내부 저장소의 `voice-notes` 디렉터리에 `.m4a` 파일로 저장됩니다. Android SpeechRecognizer는 녹음 중 live transcription 용도로만 사용됩니다. 이 점은 제품 컨셉과 기술적으로 정직하게 맞습니다.

현재 경험상 주의할 점:

- 녹음 중이라는 시각적 강도는 약합니다.
- 녹음 시간 표시가 없습니다.
- 저장 전에 "오디오가 실제로 저장되었는지"를 사용자가 명확히 알기 어렵습니다.
- SpeechRecognizer 실패 사유는 사용자에게 충분히 설명되지 않습니다.
- 전사 대기 중 기록을 나중에 수동으로 재전사하는 전용 화면은 없습니다.

## 하루 기록 화면

오늘 기록 화면은 오늘 생성된 `VoiceNote`를 시간순으로 보여줍니다.

각 카드에는 다음이 보입니다.

- 명상 기록 또는 작업 기록
- 생성 시간
- 전사 상태
- 오디오 있음/없음
- 전사 텍스트 또는 전사 대기 중

기록의 기본 정보는 확인 가능하지만, "루틴 단위"나 "명상 -> 작업" 순서로 묶어 보는 경험은 아직 부족합니다. 현재는 세그먼트별 맥락보다 노트 목록에 가깝습니다.

## 일기 경험

일기 화면은 오늘 일기 생성 버튼과 생성된 일기 내용을 보여줍니다. 오늘 화면에도 일기 생성 버튼이 있습니다.

일기 프롬프트는 완료된 전사 텍스트만 사용하도록 구성되어 있습니다. pending transcript는 제외합니다. 기록이 없으면 "오늘 기록이 없어 일기를 생성하지 않았습니다."라는 결과를 저장합니다.

현재 일기 경험은 기능적으로는 연결되어 있지만, 사용자가 "어떤 기록이 일기에 사용되었는지", "왜 생성이 안 됐는지", "pending 기록 때문에 내용이 빠졌는지"를 이해하기에는 정보가 부족합니다.

## 로컬 저장 구조

현재 로컬 저장은 다음처럼 나뉩니다.

- Room DB
  - `routine_sessions`
  - `routine_segments`
  - `voice_notes`
  - `daily_diaries`
- 앱 내부 파일 저장소
  - `voice-notes/note-{timestamp}.m4a`
- SharedPreferences
  - 일반 설정
- EncryptedSharedPreferences 우선
  - OpenAI API 키
  - 실패 시 앱 전용 SharedPreferences fallback

제품 관점에서는 핵심 데이터가 적절히 분리되어 있습니다. 다만 오디오 파일과 DB row의 lifecycle 관리, 삭제 정책, 파일 유실 시 표시 정책은 아직 명확하지 않습니다.
