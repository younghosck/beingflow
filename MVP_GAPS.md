# MVP_GAPS

## MVP로 보기 위해 빠진 것

현재 앱은 빌드되고 핵심 루프가 코드상 동작하지만, 실제 사용자에게 MVP로 배포하기 전에 보강해야 할 gap이 있습니다. 아래는 UX 흐름, 타이머 경험, 음성 메모 경험, 하루 기록 화면, 로컬 저장 구조 중심의 우선순위입니다.

## P0: 실제 사용을 막거나 신뢰를 떨어뜨리는 gap

### 1. 권한 거부 상태 UX

현재 앱은 시작 시 권한을 요청하지만, 거부됐을 때의 대체 UX가 부족합니다.

필요:

- 마이크 권한이 없으면 녹음 버튼 비활성화
- 왜 필요한지 한국어 안내
- 설정으로 이동하는 액션
- 알림 권한이 없으면 타이머 종료 알림이 제한된다는 안내

### 2. 녹음 상태 표시

음성 메모 화면에는 녹음 중 상태가 버튼 enabled 상태로만 드러납니다.

필요:

- 녹음 중 표시
- 녹음 시간
- 저장된 오디오 파일 존재 여부
- 녹음 실패 시 명확한 오류

### 3. 타이머 종료 상태 복구 UX

DB에는 `expectedEndAt`이 저장되지만, 앱이 완전히 종료된 뒤 재진입했을 때 사용자에게 "이미 시간이 끝났고 기록이 필요하다"는 경험이 더 명확해야 합니다.

필요:

- 앱 시작 시 만료된 running segment를 `NOTE_REQUIRED`로 정리
- 오늘 화면에 "기록 대기 중" 상태 표시
- 백그라운드 종료 알림 탭 시 해당 음성 기록 화면으로 이동

### 4. 오디오와 전사 저장 실패 처리

현재 저장 실패나 MediaRecorder 예외가 사용자에게 충분히 드러나지 않습니다.

필요:

- 녹음 시작 실패 처리
- 녹음 정지 실패 처리
- 0 byte 파일 처리
- 오디오 파일이 없는 note와 pending transcript의 구분

### 5. 오늘 기록의 세션/세그먼트 그룹

현재 기록 화면은 flat list입니다. BeingFlow MVP라면 적어도 오늘의 루틴 흐름대로 보여줘야 합니다.

필요:

- 세션별 그룹
- 명상 1 / 작업 / 명상 2 구분
- 완료, 스킵, 전사 대기, 전사 실패 상태
- 각 세그먼트에 연결된 기록 표시

## P1: MVP 완성도를 높이는 gap

### 6. 일기 생성 전 source visibility

사용자는 어떤 기록이 일기에 들어가는지 알 수 없습니다.

필요:

- 일기에 포함될 전사 목록 미리보기
- pending transcript 제외 안내
- OpenAI 전송 전 명시적 확인 옵션

### 7. OpenAI 없는 기본 일기/기록 모드

API 키가 없으면 일기 생성은 설정 안내로 끝납니다. MVP 사용성 관점에서는 로컬 기록만으로도 오늘의 원자료를 보여주는 fallback이 있으면 좋습니다.

필요:

- API 키 없이 "오늘 기록 모음" 생성
- OpenAI 일기와 로컬 기록 모음의 차이 표시

### 8. 전사 재시도 UX

OpenAI 전사 재시도는 hidden behavior입니다.

필요:

- 오늘 기록 화면에서 pending note 전사 재시도
- 개별 기록 전사 재시도
- 실패 사유와 마지막 시도 시간 저장

### 9. 루틴 진행 표시

현재 타이머 화면은 현재 세그먼트만 보여줍니다.

필요:

- 1/3, 2/3, 3/3 진행 표시
- 이전 단계 완료 여부
- 다음 단계 안내
- "기록 후 다음 단계 시작" 구조 강조

### 10. 설정 입력 검증

설정 화면은 숫자와 시간 문자열을 느슨하게 파싱합니다.

필요:

- 시간 범위 검증
- 음수/0분 방지
- HH:mm 형식 오류 안내
- 모델명 공백 방지

## P2: 운영과 데이터 관리 gap

### 11. 데이터 삭제/보관 정책

현재 앱 안에서 기록 삭제, 오디오 삭제, 전체 초기화가 없습니다.

필요:

- 개별 음성 기록 삭제
- 일별 데이터 삭제
- 오디오만 삭제
- 전사 후 오디오 자동 삭제 옵션

### 12. 과거 날짜 탐색

Daily Diary screen은 오늘 중심입니다.

필요:

- 날짜 선택
- 과거 일기 조회
- 과거 기록 조회

### 13. WorkManager 결과 가시화

daily worker가 실패하거나 retry해도 사용자는 알기 어렵습니다.

필요:

- 마지막 일기 생성 시도 시간
- 실패 상태 표시
- 재시도 버튼

### 14. 알림 탭 동선

타이머 종료 알림은 표시되지만 알림 탭이 앱 내 특정 기록 화면으로 깊게 연결되지 않습니다.

필요:

- pending note 화면 deep link
- notification pending intent

### 15. 로컬 DB와 오디오 파일 lifecycle 정합성

DB에는 audioPath 문자열이 저장됩니다. 파일 삭제/이동/손상 시 처리 정책이 없습니다.

필요:

- audioPath 파일 존재 확인
- 파일 누락 상태 표시
- orphan audio cleanup
- DB와 파일 백업/복원 정책

## 우선순위 제안

다음 작업 순서가 제품 MVP 완성에 가장 효과적입니다.

1. 권한 거부/녹음 실패/녹음 중 표시 보강
2. 오늘 기록을 세션과 세그먼트 기준으로 재구성
3. 앱 재진입 시 만료 타이머를 기록 대기 상태로 복구
4. 일기 생성 전 포함 기록 미리보기
5. pending 전사 재시도 UX 추가

